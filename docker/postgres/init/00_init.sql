-- PostgreSQL initialization script
-- This runs on first database creation only.
-- Grants all privileges on the database to the app user.

GRANT ALL PRIVILEGES ON DATABASE codereviewer TO codereviewer;
